import styled from "styled-components";

export const Redes = styled.div`

   margin-top: 16px;
   text-align: center;
   img{
   width: 32px;
   padding: 5px;
   transition: transform .2s;
   &:hover{
    transform: scale(1.2);
   }
   }

`